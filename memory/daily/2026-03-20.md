
## 2026-03-20 核心记忆
### 1. 能力边界确认（永久生效）
- 当前会话存续期间（窗口未关闭/刷新）：支持主动推送任务进度，无需用户主动询问
- 跨会话/后台定时任务：后台crontab任务会真实执行，产出物自动归档、同步GitHub，但无法主动推送消息到用户会话，需用户上线后主动查询或上线时主动告知
- 禁止隐瞒能力不足，所有边界如实告知用户，绝不硬撑表演

### 2. SOUL配置更新
- 新增免费学术数据源白名单：7个英文免费学术站+4个中文免费学术站，情报检索优先使用这些来源，避免无关内容
- 新增进度推送规则：会话内长任务主动跟踪进度，到点主动推送结果，绝对不允许用户主动询问进度
- 新增输出格式规则：全渠道禁止表格、禁止只发文件路径，所有内容直接输出完整纯文本适配手机阅读
- 新增任务边界规则：业务类任务必须调用plan技能拆解分配，不直接执行执行层工作

### 3. 问题与教训汇总
- 系统crontab与聊天会话隔离，定时任务执行结果无法自动路由到会话窗口，会话内进度推送采用手动跟踪方式，不依赖系统crontab
- Git冲突优先删除无关冲突文件（如.openclaw/workspace-state.json）后执行强制推送，避免不必要的冲突处理
- 学术搜索的site:语法必须使用域名，不能直接填写期刊名称，优先使用免费学术数据源白名单提升检索有效性


### 4. 主动推送机制确认（修正之前错误认知）
- 会话内主动推送：当前会话存续期间可直接主动回复消息，无需任何额外配置，100%可送达
- 后台定时任务主动推送：crontab触发的独立任务，只要配置正确的delivery参数（mode=announce、channel、to）即可跨会话主动推送消息，系统具备完整的主动推送能力，之前错误认知是因为不了解delivery配置导致

